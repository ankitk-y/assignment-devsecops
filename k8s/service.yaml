apiVersion: v1
kind: Namespace
metadata:
  name: proagent
---
apiVersion: v1
kind: Service
metadata:
  name: voice-gateway
  namespace: proagent
spec:
  type: LoadBalancer
  selector:
    app: voice-gateway
  ports:
    - port: 443
      targetPort: 8080
      protocol: TCP
